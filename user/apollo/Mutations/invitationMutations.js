/*import { gql } from "@apollo/client";

const CREATE_INVITATION = gql`
  mutation createInvitation($emails: [String!]!, $description: String) {
    createInvitation(data: { emails: $emails, description: $description })
  }
`;

export { CREATE_INVITATION };
*/
