/*import { createmail } from "../../utils/emails";

describe("Sending Mail", () => {
  test("Should return Success string", async () => {
    const result = await createmail(
      "rayajog382@100xbit.com",
      "subject",
      "html"
    );
    expect(result).toContain("Email has been sent");
  });
});
*/

test("should ", () => {
  expect(1).toBe(1);
});
